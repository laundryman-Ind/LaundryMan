<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 23+ variant of AppTheme.NoActionBar. IMPORTANT: the parent must be
         explicit — without it, aapt2 infers the parent from the style name
         ("AppTheme" → DarkActionBar) and Android shows a native action bar
         (the blue "Laundry Man" bar) on top of the app. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:background">@color/paper</item>
        <item name="android:windowBackground">@color/paper</item>
        <item name="android:statusBarColor">@color/paper</item>
        <item name="android:navigationBarColor">@color/ink</item>
    </style>
</resources>
